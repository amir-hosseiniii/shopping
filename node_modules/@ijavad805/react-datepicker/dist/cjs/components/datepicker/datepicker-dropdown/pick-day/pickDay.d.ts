/// <reference types="react" />
import "./pickDay.scss";
import { Moment } from "moment";
interface IProps {
    onStep: Function;
    onlyView?: boolean;
    customMonth?: Moment;
    onDayClick?: (date: Moment) => void;
}
declare const PickDay: ({ onStep, onlyView, customMonth, onDayClick }: IProps) => JSX.Element;
export default PickDay;
