/// <reference types="react" />
import "./style.scss";
import moment from "moment";
interface IProps {
    date: moment.MomentInput;
    disabled?: boolean;
    onClick?: () => void;
    cellIndexInWeek: number;
}
declare const Cell: ({ date, disabled, onClick, cellIndexInWeek }: IProps) => JSX.Element;
export default Cell;
